name: CI - Build and Push Containers

on:
  workflow_run:
    workflows: ["CI - Test Application"]
    types:
      - completed
  workflow_dispatch:

jobs:
  build-containers:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push TaskList API Container
        uses: docker/build-push-action@v5
        with:
          context: ./app
          file: ./app/Dockerfile
          push: true
          tags: |
            slmakomazi/tasklistapp:api-latest
            slmakomazi/tasklistapp:api-${{ github.run_number }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and Push PostgreSQL Database Container
        uses: docker/build-push-action@v5
        with:
          context: ./database
          file: ./database/Dockerfile
          push: true
          tags: |
            slmakomazi/tasklistapp:db-latest
            slmakomazi/tasklistapp:db-${{ github.run_number }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
